# Ingestion Configuration
# Copy to .env and update values

# API Configuration
API_BASE_URL=http://localhost:8000

# SECURITY: Generate a unique, secure token (minimum 16 characters)
# DO NOT use the placeholder value below - generate your own secure token!
#
# Token generation options:
#   openssl rand -hex 32
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
#   head -c 32 /dev/urandom | base64
#
# This token must match the INGEST_TOKEN configured in your API server.
INGEST_TOKEN=your-secure-token-here-min-16-chars

# For production deployment
# API_BASE_URL=https://api.dronewatch.cc

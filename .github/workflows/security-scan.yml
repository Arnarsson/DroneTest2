name: Dependency Security Scanning

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: # Allow manual trigger

jobs:
  # Python dependency vulnerability scanning using pip-audit
  python-security-scan:
    name: Python Dependency Scan (pip-audit)
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install pip-audit
        run: pip install pip-audit

      - name: Initialize scan results
        run: |
          echo "PYTHON_SCAN_FAILED=false" >> $GITHUB_ENV
          mkdir -p scan-results

      - name: Scan root requirements.txt
        id: scan-root
        continue-on-error: true
        run: |
          echo "### Scanning: requirements.txt (root)" >> $GITHUB_STEP_SUMMARY
          if pip-audit -r requirements.txt --format=columns 2>&1 | tee scan-results/root.txt; then
            echo "✅ No vulnerabilities found in root requirements.txt" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Vulnerabilities found in root requirements.txt" >> $GITHUB_STEP_SUMMARY
            echo "PYTHON_SCAN_FAILED=true" >> $GITHUB_ENV
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

      - name: Scan api/requirements.txt
        id: scan-api
        continue-on-error: true
        run: |
          echo "### Scanning: api/requirements.txt" >> $GITHUB_STEP_SUMMARY
          if pip-audit -r api/requirements.txt --format=columns 2>&1 | tee scan-results/api.txt; then
            echo "✅ No vulnerabilities found in api/requirements.txt" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Vulnerabilities found in api/requirements.txt" >> $GITHUB_STEP_SUMMARY
            echo "PYTHON_SCAN_FAILED=true" >> $GITHUB_ENV
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

      - name: Scan ingestion/requirements.txt
        id: scan-ingestion
        continue-on-error: true
        run: |
          echo "### Scanning: ingestion/requirements.txt" >> $GITHUB_STEP_SUMMARY
          if pip-audit -r ingestion/requirements.txt --format=columns 2>&1 | tee scan-results/ingestion.txt; then
            echo "✅ No vulnerabilities found in ingestion/requirements.txt" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Vulnerabilities found in ingestion/requirements.txt" >> $GITHUB_STEP_SUMMARY
            echo "PYTHON_SCAN_FAILED=true" >> $GITHUB_ENV
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

      - name: Scan frontend/requirements.txt
        id: scan-frontend
        continue-on-error: true
        run: |
          echo "### Scanning: frontend/requirements.txt" >> $GITHUB_STEP_SUMMARY
          if pip-audit -r frontend/requirements.txt --format=columns 2>&1 | tee scan-results/frontend.txt; then
            echo "✅ No vulnerabilities found in frontend/requirements.txt" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Vulnerabilities found in frontend/requirements.txt" >> $GITHUB_STEP_SUMMARY
            echo "PYTHON_SCAN_FAILED=true" >> $GITHUB_ENV
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

      - name: Scan frontend/api/requirements.txt
        id: scan-frontend-api
        continue-on-error: true
        run: |
          echo "### Scanning: frontend/api/requirements.txt" >> $GITHUB_STEP_SUMMARY
          if pip-audit -r frontend/api/requirements.txt --format=columns 2>&1 | tee scan-results/frontend-api.txt; then
            echo "✅ No vulnerabilities found in frontend/api/requirements.txt" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Vulnerabilities found in frontend/api/requirements.txt" >> $GITHUB_STEP_SUMMARY
            echo "PYTHON_SCAN_FAILED=true" >> $GITHUB_ENV
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

      - name: Python scan summary
        run: |
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "## 🐍 Python Dependency Scan Summary" >> $GITHUB_STEP_SUMMARY
          if [ "$PYTHON_SCAN_FAILED" = "true" ]; then
            echo "⚠️ **Vulnerabilities detected in one or more Python dependencies.**" >> $GITHUB_STEP_SUMMARY
            echo "Review the scan results above for details." >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ **All Python dependencies passed security scan.**" >> $GITHUB_STEP_SUMMARY
          fi

  # JavaScript dependency vulnerability scanning using npm audit
  javascript-security-scan:
    name: JavaScript Dependency Scan (npm audit)
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js LTS
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'

      - name: Initialize scan results
        run: |
          echo "JS_SCAN_FAILED=false" >> $GITHUB_ENV
          mkdir -p scan-results

      - name: Scan frontend/package.json
        id: scan-frontend
        continue-on-error: true
        working-directory: frontend
        run: |
          echo "### Scanning: frontend/package.json" >> $GITHUB_STEP_SUMMARY
          # Install dependencies to generate package-lock.json if needed
          npm install --package-lock-only 2>/dev/null || true
          if npm audit --audit-level=moderate 2>&1 | tee ../scan-results/frontend-npm.txt; then
            echo "✅ No moderate+ vulnerabilities found in frontend/package.json" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Vulnerabilities found in frontend/package.json (moderate+)" >> $GITHUB_STEP_SUMMARY
            echo "JS_SCAN_FAILED=true" >> $GITHUB_ENV
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

      - name: Scan tests/package.json
        id: scan-tests
        continue-on-error: true
        working-directory: tests
        run: |
          echo "### Scanning: tests/package.json" >> $GITHUB_STEP_SUMMARY
          # Install dependencies to generate package-lock.json if needed
          npm install --package-lock-only 2>/dev/null || true
          if npm audit --audit-level=moderate 2>&1 | tee ../scan-results/tests-npm.txt; then
            echo "✅ No moderate+ vulnerabilities found in tests/package.json" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Vulnerabilities found in tests/package.json (moderate+)" >> $GITHUB_STEP_SUMMARY
            echo "JS_SCAN_FAILED=true" >> $GITHUB_ENV
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

      - name: JavaScript scan summary
        run: |
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "## 📦 JavaScript Dependency Scan Summary" >> $GITHUB_STEP_SUMMARY
          if [ "$JS_SCAN_FAILED" = "true" ]; then
            echo "⚠️ **Vulnerabilities detected in one or more JavaScript dependencies.**" >> $GITHUB_STEP_SUMMARY
            echo "Review the scan results above for details." >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ **All JavaScript dependencies passed security scan (moderate+ severity).**" >> $GITHUB_STEP_SUMMARY
          fi
